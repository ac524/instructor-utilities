const { Credentials } = require("./Credentials");
const { RoomPatch } = require("./RoomPatch");
const { StudentPatch } = require("./StudentPatch");
const { StudentNew } = require("./StudentNew");

module.exports = {
    Credentials,
    RoomPatch,
    StudentPatch,
    StudentNew
}